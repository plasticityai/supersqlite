union - null first point
:memory: #use in-memory database
SELECT AsText(GUnion(null, MakePoint(2,3)));
1 # rows (not including the header row)
1 # columns
AsText(GUnion(null, MakePoint(2,3)))
(NULL)

