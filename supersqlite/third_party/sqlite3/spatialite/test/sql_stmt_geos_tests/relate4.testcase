Relate - bad blob second arg
:memory: #use in-memory database
SELECT Relate(geom1, zeroblob(100), "T*T***T**") FROM (SELECT GeomFromText("POINT(1 2)") as geom1, GeomFromText("POINT(1 2)") as geom2) dummy;
1 # rows (not including the header row)
1 # columns
Relate(geom1, zeroblob(100), "T*T***T**")
-1
