makecircle11 - NULL srid
:memory: #use in-memory database
SELECT ST_Length(MakeCircle(0, 0, 100, NULL));
1 # rows (not including the header row)
1 # columns
ST_Length(MakeCircle(0, 0, 100, NULL))
(NULL)

