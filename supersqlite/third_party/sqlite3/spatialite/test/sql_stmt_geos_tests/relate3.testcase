Relate - non blob first arg
:memory: #use in-memory database
SELECT Relate("hello", geom2, "T*T***T**") FROM (SELECT GeomFromText("POINT(1 2)") as geom1, GeomFromText("POINT(1 2)") as geom2) dummy;
1 # rows (not including the header row)
1 # columns
Relate("hello", geom2, "T*T***T**")
-1
