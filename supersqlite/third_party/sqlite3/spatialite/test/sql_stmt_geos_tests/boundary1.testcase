Boundary - Point
:memory: #use in-memory database
SELECT AsText(Boundary(GeomFromText("POINT(1 2)")))
1 # rows (not including the header row)
1 # columns
AsText(Boundary(GeomFromText("POINT(1 2)")))
(NULL)