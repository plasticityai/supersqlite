difference - two common points
:memory: #use in-memory database
SELECT AsText(Difference(Makepoint(1,2), MakePoint(1,2)));
1 # rows (not including the header row)
1 # columns
AsText(Difference(Makepoint(1,2), MakePoint(1,2)))
(NULL)